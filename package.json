{
  "name": "onr-react-ui",
  "private": true,
  "workspaces": [
    "examples/*",
    "examples/**/packages/*",
    "examples/**/plugins/*",
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel --ignore next-starter",
    "dev": "lerna run dev --parallel --ignore next-starter",
    "build": "lerna run build --stream",
    "storybook": "lerna run storybook --stream",
    "lint": "lerna run lint --parallel",
    "format": "lerna run format --parallel",
    "test": "lerna run test --parallel",
    "prepublishOnly": "yarn build",
    "postinstall": "husky install"
  },
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "@onr/prettier-config": "^1.1.1"
  },
  "prettier": "@onr/prettier-config",
  "lint-staged": {
    "**/*.(js|ts|tsx)": [
      "eslint -c .eslintrc.js --fix",
      "prettier --write"
    ]
  }
}
