export type Theme = {
  primaryColor: string;
  normalColor: string;
  backgroundColor: string;
  darkColor: string;
  textColor: string;
  textColorSecondary: string;
  successColor: string;
  infoColor: string;
  warningColor: string;
  errorColor: string;
  codeFamily: string;
  borderColorBase: string;
  borderRadiusBase: string;
};
