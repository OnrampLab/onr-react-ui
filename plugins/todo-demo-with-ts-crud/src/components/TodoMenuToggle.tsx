import { FiBarChart } from 'react-icons/fi';

export const todoMenuToggle = (
  <FiBarChart
    size={20}
    strokeWidth={1}
    color="var(--brand-primaryColor)"
    style={{
      transform: 'rotate(90deg)',
      padding: '0 10px',
    }}
  />
);
